# Config aislada de Sway para MEAL
set $mod Mod4

exec dms run
exec sh -c 'test -f ~/.meal-bienvenida-mostrada || kitty -e meal-bienvenida'

bindsym $mod+Return exec kitty
bindsym $mod+d exec fuzzel
bindsym $mod+q kill
bindsym $mod+m exec kitty -e meal-menu
