# pacstrap-meal.conf - config del modulo shellprocess que hace la instalacion real
# Mismo patron que el "pacstrap_calamares" de EndeavourOS: instalacion en vivo
# via pacstrap, no una imagen pre-armada.

dontChroot: true
timeout: 1800
script:
  - "scripts/postinstall/pacstrap-meal.sh"
